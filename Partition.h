#pragma once
//--------------------------------------------------------------------------------
class Partition
{
	public:
		Partition();
		Partition(int width, int height);
		Partition(int x, int y, int width, int height);
		int x, y, w, h;
};
//--------------------------------------------------------------------------------
